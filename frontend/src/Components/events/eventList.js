import { Button,
    Table, 
    TableBody,
    Paper, 
    TableRow, 
    TableCell, 
    CircularProgress, 
    TableHead} from "@mui/material";

const EventList = ({ event, index }) => {
    return (
        <></>
    );
}

export default EventList;